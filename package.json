{
  "name": "ForwardEngineer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm install --dir backend && pnpm install --dir frontend && pnpm --dir frontend build",
    "start": "pnpm --dir backend start"
  }
}
